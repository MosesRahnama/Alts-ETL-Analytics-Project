# Worksheets

Human review slices used to reach fund and manager identity decisions and remaining attribute spelling splits.

| File | Role |
|---|---|
| `fund-part-01.csv` | One normalizer's slice of fund-names-matrix.csv, rows sorted by printed name; folded back by `merge`. |
| `fund-part-02.csv` | One normalizer's slice of fund-names-matrix.csv, rows sorted by printed name; folded back by `merge`. |
| `fund-part-03.csv` | One normalizer's slice of fund-names-matrix.csv, rows sorted by printed name; folded back by `merge`. |
| `fund-part-04.csv` | One normalizer's slice of fund-names-matrix.csv, rows sorted by printed name; folded back by `merge`. |
| `fund-part-05.csv` | One normalizer's slice of fund-names-matrix.csv, rows sorted by printed name; folded back by `merge`. |
| `fund-part-06.csv` | One normalizer's slice of fund-names-matrix.csv, rows sorted by printed name; folded back by `merge`. |
| `fund-part-07.csv` | One normalizer's slice of fund-names-matrix.csv, rows sorted by printed name; folded back by `merge`. |
| `fund-part-08.csv` | One normalizer's slice of fund-names-matrix.csv, rows sorted by printed name; folded back by `merge`. |
| `fund-part-09.csv` | One normalizer's slice of fund-names-matrix.csv, rows sorted by printed name; folded back by `merge`. |
| `manager-01-a.csv` | One blind searcher's slice of manager-queue.csv, its own two columns only; folded back by `manager-merge`. |
| `manager-01-b.csv` | One blind searcher's slice of manager-queue.csv, its own two columns only; folded back by `manager-merge`. |
| `manager-02-a.csv` | One blind searcher's slice of manager-queue.csv, its own two columns only; folded back by `manager-merge`. |
| `manager-02-b.csv` | One blind searcher's slice of manager-queue.csv, its own two columns only; folded back by `manager-merge`. |
| `attribute-conflicts.csv` | Funds whose remaining printed vintage, strategy, asset class, or geography labels still disagree; folded back by `fund_attributes merge`. Header-only: every printed split is settled, and the standing brief stays. |
