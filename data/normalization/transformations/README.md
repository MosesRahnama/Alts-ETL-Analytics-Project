# Decision matrices

Rule-family decision matrices for 420 inventoried data-decision sites. Rows name the input, output, context, decision owner, and former code location; src/common/matrices.py reads them and stage 20 checks them. Four non-data sites have structure dispositions in ledgers/pipeline/migration-dispositions.csv.

| File | Role |
|---|---|
| `adjudication-conventions.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `adjudication-decision-semantics.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `adjudication-duplicate-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `adjudication-outcomes.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `adjudication-resolution-routing.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `agreement-definition.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `alias-evidence-aggregation.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `analytics-quality-gate.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `assumption-unit-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `assumption-value-channel.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `attribute-canon-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `attribute-origin-vocabulary.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `attribute-spelling-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `attribute-status-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-field-fallbacks.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-index-construction.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-join-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-malformed-share.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-master-selection.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-periodicity.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-policy-summary.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-promotion-labels.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-return-eligibility.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-return-formula.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-return-window.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-source-priority.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `benchmark-use-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `calibration-candidate-status.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `calibration-formulas.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `calibration-input-cleaning.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `calibration-metric-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `calibration-output-labels.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `calibration-panel-gates.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `calibration-panel.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `calibration-source-panel.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `calibration-strategy-scope.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `cashflow-admission.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `cashflow-aggregation.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `cashflow-date-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `cashflow-fund-attribution.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `cashflow-selection.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `cashflow-sign-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `category-column-type.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `category-field-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `closed-vocabularies.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `collision-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `column-kind.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `completion-target-fields.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `conflict-severity.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `context-alias-fallback.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `context-grouping-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `coverage-layout-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `coverage-metadata-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `coverage-population.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `csv-lineage-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `csv-value-serialization.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `currency-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `currency-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `currency-token-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `database-missing-input-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `database-null-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `date-precision-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `date-role-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `date-role-precision.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `date-shapes.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `daycount-convention.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `decision-vocabulary.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `defect-definitions.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `deterministic-unit-interval.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `dimension-values.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `dispatch-scope-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `doc-type-families.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `doc-type-route.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `document-manager-identity-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `document-manager-role.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `document-manager-source-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `document-reporting-fund-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `document-row-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `document-summary-field-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `eligibility-signals.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `empty-table-reasons.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `entity-name-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `entity-prefix-routing.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `entity-resolution-status.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `evidence-anchor-format.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `evidence-field-fallbacks.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `evidence-preference-order.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `excluded-scope.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `extraction-lane-selection.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `extraction-semantic-checks.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `family-pivot.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `family-signature.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `field-descriptions.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `field-fallback-order.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `field-label-census.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `file-type-checks.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fixture-observation-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fixture-parameters.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fixture-portfolio-schemes.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fixture-position-shape.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fixture-schedule-shape.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fixture-strategy-parameters.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-attribute-scope-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-currency-weights.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-evidence-priority.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-manager-inheritance.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-manager-selection.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-master-duplicate-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-master-field-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-master-field-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-master-field-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-master-fill-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-master-input-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-master-lineage-routing.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-master-provenance.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-master-retirement-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-master-source-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-period-admission.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-position-scope.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-size-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-strategy-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-term-admission.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-term-clause-admission.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-term-collision-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-term-context-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fund-vintage-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `fx-to-usd.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `gap-ledger-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `governed-outputs.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `grid-geometry.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `grid-thresholds.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `guide-counts.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `header-stopwords.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `holding-collision-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `holding-column-source.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `holding-context-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `holding-field-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `holding-fund-attribution.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `holding-value-source.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `horizon-scope.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `identifier-composition.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `identifier-generation-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `integrated-cashflow-schedule.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `integrated-date-parsing.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `integrated-fund-universe-sources.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `integrated-identifier-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `integrated-input-cell-normalization.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `integrated-number-parsing.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `integrated-numeric-formatting.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `integrated-output-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `integrated-parameter-formulas.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `integrated-parameter-populations.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `integrated-reconciliation-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `key-normalization.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `label-normalization-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `legal-noise.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `lineage-delimiter-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `lineage-ignored-fields.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `load-null-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `manager-auto-settle-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `manager-locus-labels.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `manager-master-field-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `manager-master-source-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `manifest-roles.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `market-promotion-statuses.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `measurability-rule.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `metric-catalogue-status.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `metric-identifier-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `metric-measure-basis.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `metric-semantic-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `metric-to-cashflow-type.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `metric-to-holding-column.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `metric-to-period-column.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `metric-value-kind-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `metric-vocabulary.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `mock-defect-rate.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `model-attribution.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `money-detection.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `month-names.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `name-hint-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `name-keyword-strategy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `name-match-key.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `name-status-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `new-fund-master-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `new-fund-master-fields.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `non-names.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `null-like-values.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `number-format-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `number-pattern.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `numeric-serialization.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `numeric-text-format.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `numeric-token.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `observation-columns.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `observation-field-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `ocr-settings.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `page-data-status.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `page-row-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `page-status-classification.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `pair-key.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `pair-realignment.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `parameter-input-key-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `parameter-provenance-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `parameter-status-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `parameter-unit-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `path-normalization.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `period-column-value-kind.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `period-completion-parameters.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `period-selection-order.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `pipeline-gates.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `pipeline-runtime-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `portfolio-perspective.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `position-key.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `presentation-patterns.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `printed-label-to-category.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `printed-number-words.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `printed-precision-fields.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `printed-precision-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `printed-tokens.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `promotion-decision-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `promotion-evidence-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `promotion-fixed-fields.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `promotion-gate-route.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `promotion-gated-tables.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `promotion-missing-table-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `promotion-schema-additions.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `provenance-assignment.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `provenance-gate-scope.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `provenance-vocabulary.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-benchmark-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-column-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-copy-action.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-date-columns.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-duplicate-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-file-filters.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-identifiers.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-parsing-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-quality-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-quality-status.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-rights-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-rights-status.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-row-filters.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-source-priority.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-source-selection.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-status-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-summary-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `public-market-timezone-status.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `quality-approval-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `quality-default-tolerances.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `quality-rule-severity.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `quality-skip-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `rate-unit-conversion.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `receipt-note-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `record-columns.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `record-families.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `record-family-routing.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `record-preference-order.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `record-status-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `reference-source-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `release-expectations.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `retired-fields.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `return-construction.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-disagreement-aggregation.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-document-inclusion.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-fact-key-fields.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-fact-match-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-lineage-field-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-lineage-match-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-list-parsing.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-output-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-sampling.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-selection-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-status-to-summary-column.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-summary-formulas.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `review-trace-sampling.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `reviewer-promotion-status.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `round02-source-decision.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `rounding-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `routing-status.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `run-identifiers.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `sampling-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `scale-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `scale-words.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `solver-settings.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `source-date-order.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `source-master-fields.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `source-metadata-duplicate-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `source-path-parsing.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `source-path-selection.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `source-prefix-gate-routing.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `split-number-rules.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `status-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `strategy-benchmark-map.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `strategy-benchmark.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `strategy-income-yield.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `strategy-liquidity.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `strategy-volatility.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `subject-kind.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `subject-type-entity-kind.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `summary-statistics.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `table-cell-rule.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `table-record-id-fields.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `table-source-field.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `template-patterns.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `term-to-fund-terms-column.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `term-vocabulary.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `terms-generation-parameters.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `text-categories.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `text-layer-classification.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `text-sanitization.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `text-value-fallback.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `ticker-classification.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `ticker-pme-role.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `unit-scale-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `unit-scale.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `unmapped-observation-dispositions.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `unmatched-entity-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `value-kind-classification.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `value-kind-defaults.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `value-kind-parse.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `value-repair.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `vintage-parse.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `weight-quantum.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `wide-cell-value-source.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `wide-collision-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `wide-column-grouping.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `wide-column-renames.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `wide-context-precedence.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `wide-grain.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `wide-scale-status.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `wide-unparsed-value-policy.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |
| `xirr-excluded-cashflow-types.csv` | Decision rows for one rule family: each row names the input, output, context where needed, decision owner, and former code location. |

Next: [Matrix checks and migration evidence](../../../ledgers/pipeline/README.md).
