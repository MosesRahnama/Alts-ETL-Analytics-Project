# Wide

One modelling table per record family: a printed table row per row, a vocabulary name per column (the family's usual names, then any other name the facts carry in it), every cell traceable to fact_observation.

| File | Role |
|---|---|
| `MANIFEST.csv` | Row counts and file membership for this generated stage. |
| `bridge_pivot_observation.csv` | One row per (pivot row, observation) for the wide tables and fact_holding, with a foreign key to fact_observation. |
| `wide_allocation_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_cash_flow_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_ddq_quantitative_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_document_context.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_fee_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_financial_statement_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_financing_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_fund_economics_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_legal_clause.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_legal_term.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_nav_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_performance_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_position_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_stewardship_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_stewardship_policy.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_subscription_reference.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
| `wide_valuation_observation.csv` | One printed table row per row, one vocabulary name per column, with the observation IDs each row was built from. |
